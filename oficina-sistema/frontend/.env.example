# Backend novo (Next.js + Supabase Auth/RLS) — módulo de autenticação e,
# progressivamente, os demais módulos migrados.
VITE_BACKEND_URL=http://localhost:3001

# Backend antigo (FastAPI) — ainda usado pelos módulos não migrados.
VITE_API_URL=http://localhost:8000

# Supabase Auth — login acontece direto do browser contra o Supabase,
# sem passar pelo backend. A chave anon é pública por design.
VITE_SUPABASE_URL=https://lbkwphwwgukdzwdhzvbn.supabase.co
VITE_SUPABASE_ANON_KEY=cole-a-chave-anon-aqui
